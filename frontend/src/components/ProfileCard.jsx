export default function ProfileCard({ user, action }) { return <article className="profile-card"><div className="profile-card__cover" /><div className="profile-card__body">{user?.picture ? <img src={user.picture} alt="" /> : <span>{user?.name?.[0] || "U"}</span>}<div><strong>{user?.name || "Community member"}</strong><small>{user?.location || "Desire Link"}</small></div>{action}</div></article>; }
