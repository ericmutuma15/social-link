import { Link } from "react-router-dom";
import { useAuth } from "../context/AuthContext";
const topics = ["#CreativeMinds", "#WeekendPlans", "#DesignInspiration"];
export default function RightSidebar() { const { user } = useAuth(); return <aside className="right-sidebar"><section className="sidebar-card"><p className="eyebrow">YOUR SPACE</p><h3>Good evening, {user?.name?.split(" ")[0] || "there"}.</h3><p className="muted">Share something worth remembering.</p><Link className="text-link" to="/create-post">Create a post →</Link></section><section className="sidebar-card"><div className="side-title"><h3>Trending now</h3><Link to="/explore">See all</Link></div>{topics.map((topic, index) => <div className="trend" key={topic}><span>{topic}</span><small>{(index + 2) * 128} posts</small></div>)}</section><section className="sidebar-card"><div className="side-title"><h3>Who to follow</h3><Link to="/people">Discover</Link></div><div className="follow-row"><span className="mini-avatar">A</span><div><strong>New connections</strong><small>People in your community</small></div></div></section></aside>; }
